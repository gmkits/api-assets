/**
 * Runtime-facing aliases for the single `.hday` v2 codec owned by
 * `@holiday/spec`.
 */
export {
  HdayFormatError,
  parseHdayBundle,
} from '@holiday/spec';

export type {
  HdayDayEntry as DayEntry,
  HdayHeader,
  HdayNameListEntry as NameListEntry,
  ParsedHdayBundle as HdayBundle,
} from '@holiday/spec';
