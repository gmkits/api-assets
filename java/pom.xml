<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.github.gmkits</groupId>
    <artifactId>cn-holiday-kit-java</artifactId>
    <version>1.0.0-rc1</version>
    <packaging>pom</packaging>

    <name>cn-holiday-kit-java</name>
    <description>cn-holiday-kit 内部构建反应堆；对外仅发布单一 Java JAR</description>

    <modules>
        <module>holiday-spec-java</module>
        <module>holiday-core-java</module>
        <module>holiday-lunar-java</module>
        <module>cn-holiday-kit</module>
        <module>holiday-benchmarks</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>8</maven.compiler.source>
        <maven.compiler.target>8</maven.compiler.target>
        <maven.compiler.release>8</maven.compiler.release>
        <junit.version>5.10.2</junit.version>
        <maven.javadoc.version>3.12.0</maven.javadoc.version>
        <maven.shade.version>3.6.0</maven.shade.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven.javadoc.version}</version>
                    <configuration>
                        <doclint>all</doclint>
                        <failOnWarnings>true</failOnWarnings>
                        <encoding>UTF-8</encoding>
                        <docencoding>UTF-8</docencoding>
                        <charset>UTF-8</charset>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <!-- 内部模块测试也必须读取唯一的通用日历资产。 -->
                    <systemPropertyVariables>
                        <cn.holiday.assets.path>${maven.multiModuleProjectDirectory}/../data/date-assets</cn.holiday.assets.path>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- 核心库保持 Java 8；HTTP 服务只在受支持的 Java 17+ 上参与构建。 -->
        <profile>
            <id>holiday-api-java17</id>
            <activation>
                <jdk>[17,)</jdk>
            </activation>
            <modules>
                <module>holiday-api</module>
            </modules>
        </profile>
    </profiles>

</project>
