{
  "name": "cn-holiday-kit",
  "version": "1.0.0-rc.1",
  "private": true,
  "description": "Holiday Data Platform — cross-platform holiday toolkit for China",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "0.28.1",
      "postcss": "8.5.25"
    }
  }
}
